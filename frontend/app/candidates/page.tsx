import {AppShell} from '@/components/AppShell';import {CandidateWorkspace} from '@/components/CandidateWorkspace';export default function Page(){return <AppShell><div className="page-heading"><div><span className="eyebrow">Lodge operations</span><h1>Candidates</h1><p>Manage candidate profiles, mentors, stage history, tasks and progression evidence.</p></div></div><CandidateWorkspace/></AppShell>}
