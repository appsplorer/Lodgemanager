'use client';import Link from 'next/link';import {useState} from 'react';import {api} from '@/lib/api';import {bindOfflinePrincipal} from '@/lib/offline';
export default function Login(){const[msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await api<{mfa_required?:boolean;mfa_enrollment_required?:boolean;user?:{id:string|number}}>('/auth/login',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password')})},'');if(r.mfa_required)sessionStorage.setItem('lodgeflow:mfa-enroll',r.mfa_enrollment_required?'1':'0');else if(r.user?.id)await bindOfflinePrincipal(r.user.id);location.assign(r.mfa_required?'/mfa':'/dashboard')}catch(e){setMsg(e instanceof Error?e.message:'Sign in failed')}}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><div className="brand-mark">LF</div><span className="eyebrow">Private lodge office</span><h1>Welcome back</h1><p>Sign in to your LodgeFlow workspace.</p><label>Email<input type="email" name="email" autoComplete="username" required/></label><label>Password<input type="password" name="password" autoComplete="current-password" required/></label><button className="primary large">Sign in securely</button>{msg&&<div className="alert error" role="alert">{msg}</div>}<div className="auth-links"><Link href="/forgot-password">Forgot your password?</Link></div></form></main>}
