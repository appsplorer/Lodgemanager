import Link from 'next/link';export default function Offline(){return <main className="offline-page"><div className="offline-card"><div className="brand-mark">LF</div><span className="eyebrow">Offline continuity</span><h1>You’re working offline.</h1><p>LodgeFlow can reopen previously visited workspace screens and encrypted local data snapshots. Routine edits are queued and automatically synchronized when the connection returns.</p><p><b>For safety:</b> billing, credentials, MFA, provider payments, file uploads and governed approvals remain online-only.</p><Link className="primary large" href="/dashboard">Open cached workspace</Link></div></main>}
