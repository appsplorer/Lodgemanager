'use client';
import {useEffect} from 'react';
export function MarketingMotion(){useEffect(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduce){document.querySelectorAll('[data-reveal]').forEach(el=>el.classList.add('is-visible'));return;}const io=new IntersectionObserver(entries=>{for(const e of entries)if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}},{threshold:.14,rootMargin:'0px 0px -7%'});document.querySelectorAll('[data-reveal]').forEach(el=>io.observe(el));const move=(e:PointerEvent)=>{document.querySelectorAll<HTMLElement>('[data-parallax]').forEach(el=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.setProperty('--mx',`${x*5}deg`);el.style.setProperty('--my',`${y*-5}deg`)})};window.addEventListener('pointermove',move,{passive:true});return()=>{io.disconnect();window.removeEventListener('pointermove',move)}},[]);return null;}
